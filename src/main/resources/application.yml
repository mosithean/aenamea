app:
  datasource:
    main:
      driver-class-name: org.postgresql.Driver
      jdbc-url: jdbc:postgresql://localhost:5432/msthn
      password: 'password'
      pool-size: 30
      username: msthn

server:
  error:
    include-binding-errors: always
    include-message: always


